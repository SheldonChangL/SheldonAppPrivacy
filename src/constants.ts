/**
 * Configurable constants for the Privacy Policy page.
 * Update these values before publishing.
 */

/** The public-facing name of the Android application. */
export const APP_NAME = "Home Asset Keeper";

/**
 * The contact email address for privacy inquiries and data deletion requests.
 * Replace with your real address before going live.
 */
export const CONTACT_EMAIL = "privacy@yourdomain.com";

/** The date this policy was last reviewed and published. */
export const LAST_UPDATED = "March 4, 2026";

/** Android package name — used in Data Deletion instructions. */
export const PACKAGE_NAME = "chang.sllj.homeassetkeeper";

/** Developer / company name as it appears on Google Play. */
export const DEVELOPER_NAME = "Sheldon Chang";

/** Google Play Store listing URL (fill in after publishing). */
export const PLAY_STORE_URL = `https://play.google.com/store/apps/details?id=${PACKAGE_NAME}`;
